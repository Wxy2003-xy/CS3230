#ifndef M_SORT
#define M_SORT
#include <bits/stdc++.h>
using namespace std;
void sort_inplace(vector<int>& n);
void split(vector<int>& ori, vector<int>& seg);
void merge(vector<int>& m, vector<int>& n);
vector<int> sort_immu(vector<int>& n);
#endif